## 1. Кнопки Pull, "Выгрузить в файловую систему" и "Обновить реестр" (`KnGitGuiMessageBox`)

- [x] 1.1 В `renderPullButton()` раскомментировать и использовать существующую строку `caption: "Получить изменения с сервера"`; иконку (`imageConfig`) не задавать вовсе — кнопка только текстовая; `style` остаётся `"transparent"`, видимая рамка/заливка реализована CSS в задаче 2.1, а не через `style`. Нажатие по-прежнему вызывает `pull` (через `onMessageBoxPullButtonClick`) — обработчик не менялся.
- [x] 1.2 В `renderDownloadChangesToFSButton()` добавить `caption: "Выгрузить в файловую систему"`; иконку (`imageConfig`) не задавать вовсе. `style` остаётся `"transparent"`. Нажатие по-прежнему вызывает `downloadChangesToFS` — обработчик не менялся.
- [x] 1.3 `renderRefreshButton()` не менялся по составу конфига (уже был `caption`-only, без иконки); стиль оформления вынесен в CSS (задача 2.1).

## 2. Видимый стиль и компактное расположение трёх кнопок

- [x] 2.1 В `Schemas/KnGitGuiMessageBox/KnGitGuiMessageBox.less` добавить рамку/заливку/паддинг на контейнеры `#KnGitGuiMessageBox-refresh-button` / `-reload-button` / `-pull-button` (те, что создаются в `getTpl()`, а не на внутреннем `-wrapperEl` кнопки `BPMSoft.Button` — первая попытка через `-wrapperEl` дала рамку не для всех трёх кнопок одинаково). `.header-with-button` получил `align-items: center; flex-wrap: wrap; gap: 8px`. Правила размера иконок (`#GitGuiDownloadChangesToFSButton-imageEl` / `#GitGuiMessageBoxPullButton-imageEl`) удалены — иконок больше нет. Платформенный `ButtonEnums.style.DEFAULT` пробовали и отклонили (слишком крупная кнопка, обрезание иконки) — не менять `style` в JS обратно на него без явного запроса. По отзыву пользователя ("очень большие по высоте") паддинг контейнеров уменьшен до `2px 10px`, добавлен `line-height: normal`, и внутренним элементам кнопки (`#...-button *`) обнулены `padding`/`margin`, так как высота определялась в основном внутренними отступами `BPMSoft.Button`, а не контейнером.
- [x] 2.4 По отзыву пользователя (при узком экране/открытых devtools кнопки "разваливались" и наезжали на заголовки реестра) разделить в `getTpl()` контейнер `.header-with-button` на заголовок `{id}-message` и отдельную группу `.header-buttons-group`, оборачивающую три кнопки. `.header-with-button` — `justify-content: space-between; flex-wrap: wrap; margin-bottom: 8px`, так что при нехватке места вся группа кнопок целиком переносится на новую строку под заголовком, а не разваливается кнопка за кнопкой. `.header-buttons-group` — свой `display: flex; flex-wrap: wrap; gap: 8px`, чтобы кнопки внутри группы тоже корректно переносились между собой при совсем узком экране.
- [x] 2.2 Проверить визуально при обычной ширине диалога (80vw) и при узком экране/открытых devtools, что три кнопки не обрезаны, не перекрываются друг с другом, с текстом "Незафиксированные изменения" или с заголовками реестра ниже, и что рамка видна у всех трёх одинаково (включая "Обновить реестр"). Проверено пользователем вручную.
- [x] 2.3 Положение контейнеров `KnGitGuiMessageBox-reload-button` / `-pull-button` / `-refresh-button` не изменилось (только стили), поэтому `data-tour` селекторы в `getTpl()` не менялись. Прохождение тура для визуального подтверждения — часть ручной проверки (см. 2.2/3.1).

## 3. Ручная проверка

**Все задачи этого раздела требуют живого окружения BPMSoft (браузер, авторизация, задеплоенный пакет) — проверены пользователем вручную после развёртывания.**

- [x] 3.1 В диалоге коммита убедиться, что Pull, "выгрузить в файловую систему" и "Обновить реестр" показывают только текстовую подпись с одинаковым видимым стилем (рамка/заливка), без иконок.
- [x] 3.2 Нажать все три кнопки и убедиться, что существующее поведение (`pull`, `downloadChangesToFS`, `refresh` — вызовы сервисов, сообщения об успехе/ошибке) не изменилось.
- [x] 3.3 Убедиться, что кнопка в шапке, "Закрыть", "Помощь", "Коммит" и "Push" визуально и функционально не изменились по сравнению с состоянием до этого изменения.

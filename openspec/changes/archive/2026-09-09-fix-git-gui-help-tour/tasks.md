## 1. Порядок и состав шагов тура

- [x] 1.1 В массиве `steps` (`onHelpButtonClick`, `KnGitGuiMessageBox.js`) на позиции текущего шага `download-changes-button` вставить три подряд идущих шага в порядке "Обновить реестр" → "выгрузить в файловую систему" → Pull: добавить новый шаг для `[data-tour="refresh-button"]`, оставить существующий шаг `download-changes-button`, перенести шаг `pull-button` из конца массива в эту группу. Проверка: в исходном коде `steps` три соответствующих объекта идут подряд в указанном порядке, и шаг для `refresh-button` больше не отсутствует.

## 2. Защита от повторного запуска тура

- [x] 2.1 В начале `onHelpButtonClick` добавить проверку `modal.querySelector(".tour-layer")` — если оверлей уже есть, выйти без создания нового. Проверка: повторный вызов `onHelpButtonClick` при уже существующем `.tour-layer` не добавляет второй элемент с этим классом в DOM.
- [x] 2.2 В `onCloseMessageBoxButtonClick` перед вызовом `this.hide()` найти и удалить `.tour-layer` внутри модалки, если он есть. Проверка: после закрытия диалога во время активного тура элемент `.tour-layer` отсутствует в DOM.

## 3. Ручная проверка

- [x] 3.1 Открыть диалог, нажать "Помощь" и пройти тур до конца — убедиться, что после "Незафиксированные изменения" (`unstaged`) шаги идут в порядке "Обновить реестр" → "выгрузить в файловую систему" → Pull, и что у "Обновить реестр" есть содержательное описание.
- [x] 3.2 Во время активного тура повторно нажать "Помощь" несколько раз подряд — убедиться, что экран не темнеет сильнее, текущий шаг тура не сбрасывается и второй попап не появляется.
- [x] 3.3 Закрыть диалог кнопкой "Закрыть" во время активного тура, открыть диалог заново и нажать "Помощь" — убедиться, что виден только один тур, без оверлея от предыдущего запуска.

## 1. Обработка ошибки при открытии Git GUI

- [x] 1.1 В `prepareCollection` (`Schemas\KnGitGuiMixin\KnGitGuiMixin.js`) добавить проверку отсутствия `Log` в ответе `GetRepoStatusWithSchemas` и вызов `BPMSoft.showErrorMessage(...)` с универсальным текстом-подсказкой проверить настройку пути до репозитория, не открывая диалог
- [x] 1.2 Проверить вручную три сценария: пустая настройка `KnAutocommiterRepoPath`, каталог не является git-репозиторием, корректный путь — убедиться, что уведомление показывается только в первых двух случаях и диалог по-прежнему открывается в третьем
